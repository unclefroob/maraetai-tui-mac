[workspace]
resolver = "2"
members = ["common", "daemon", "tui"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[workspace.dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "signal", "fs", "io-util", "process"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
zbus = { version = "4", default-features = false, features = ["tokio"] }
clap = { version = "4", features = ["derive"] }
directories = "5"
md-5 = "0.10"
# keyring's actual backend feature is picked per-target in common/Cargo.toml
# (Linux needs a different one than macOS) — see the comment there. No
# `keyring` entry here: a shared workspace dependency can't vary its
# feature set by target.
thiserror = "1"
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
rand = "0.8"

maraetai-common = { path = "common" }
